import { BlackBackDirective } from './black-back.directive';

describe('BlackBackDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackBackDirective();
    expect(directive).toBeTruthy();
  });
});
